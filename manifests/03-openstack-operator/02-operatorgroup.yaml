# WHAT: OperatorGroup scoping the openstack-operator subscription to the openstack-operators namespace.
# VERIFY: oc get operatorgroup -n openstack-operators
apiVersion: operators.coreos.com/v1
kind: OperatorGroup
metadata:
  name: openstack-operators-og
  namespace: openstack-operators
spec:
  upgradeStrategy: Default
